# BenchTest-Stresstes
Kiểm thử CPU, RAM, DISK trong bằng script

Cài đặt gói sysbench bằng lệnh

- sudo dpkg -i sysbench_1.0.20+ds-1_amd64.deb
- sudo apt-get -f install

Cấp quyền chạy cho file

- sudo chmod +x tress_test.sh

Chạy lệnh

- ./stress_test.sh

